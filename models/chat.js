// const Chat = require('./chatModels');

// const addUser = async (date) => {
//     const name = date.name.trim().toLowerCase();
//     const chat = date.chat.trim().toLowerCase();
//     const existingUser = await Chat.findOne({ 'comments.user.username': name });
//     if (existingUser) {
    
//   } else {
    
//   }
// }